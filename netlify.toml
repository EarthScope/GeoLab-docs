[build]
  publish = "publish"
  command = """
    pip install -r requirements.txt && \
    jupyter book build --html && \
    mkdir -p publish/onramp/geolab && \
    mv _build/html/* publish/onramp/geolab/
  """

[build.environment]
  BASE_URL = "/onramp/geolab"
  PYTHON_VERSION = "3.12"

